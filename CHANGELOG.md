# 0.1.0

- First release. Marten Studio is an embeddable Blazor Server admin UI for MartenDB, shipped as one
  Razor Class Library: `AddMartenStudio()` in the container and `MapMartenStudio()` in the pipeline
  mount it inside an existing ASP.NET Core application, against that application's own configured
  `IDocumentStore`.
